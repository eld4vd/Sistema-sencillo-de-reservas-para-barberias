import ProductShowcase from "../components/public/Tienda/ProductShowcase";

const Tienda = () => {
	return (
		<main className="bg-[#111111] text-[#FAF8F3] min-h-screen">
			<ProductShowcase />
		</main>
	);
};

export default Tienda;
