########################################
# PLANTILLA DE VARIABLES DE ENTORNO - FRONTEND
# Copia este archivo como .env y ajusta los valores según tu entorno
########################################

# --- URL del Backend API ---
# URL base del backend NestJS (sin slash final)
# Desarrollo local: http://localhost:3000/api/v1
# Producción: https://api.tudominio.com/api/v1
VITE_API_URL=http://localhost:3000/api/v1

# --- CSRF Cookie Name ---
# Nombre de la cookie CSRF expuesta por el backend (debe coincidir con backend)
# Valor por defecto: csrf_token
VITE_CSRF_COOKIE_NAME=csrf_token
