 ##############################
 # 🔹 MODO PRODUCCIÓN
 ##############################
 server {
     listen 80;
     listen [::]:80;
     listen 443 ssl;
     listen [::]:443 ssl;
     server_name eld4vd.me www.eld4vd.me;

     ssl_certificate /etc/letsencrypt/live/eld4vd.me/fullchain.pem;
     ssl_certificate_key /etc/letsencrypt/live/eld4vd.me/privkey.pem;

     # Redirección automática a HTTPS
     if ($scheme != "https") {
         return 301 https://$host$request_uri;
     }

     # ✅ API (proxy al backend NestJS)
     location /api/v1/ {
         proxy_pass http://backend:3000/api/v1/;
         proxy_set_header Host $host;
         proxy_set_header X-Real-IP $remote_addr;
         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
         proxy_set_header X-Forwarded-Proto $scheme;
     }

     # ✅ FRONTEND React + Vite (archivos estáticos)
     location / {
         root /usr/share/nginx/html;
         index index.html;
         try_files $uri /index.html;
     }

     error_log /var/log/nginx/error.log;
     access_log /var/log/nginx/access.log;
 }


###############################
## 🔹 MODO DESARROLLO-PRUEBAS (ejemplo local)
###############################
#server {
#    listen 80;
#    server_name localhost;
#
#    # Proxy al backend local (NestJS corriendo en tu PC o contenedor)
#    location /api/v1/ {
#        proxy_pass http://backend:3000/api/v1/;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
#
#    # Servir el frontend localmente
#    location / {
#        root /usr/share/nginx/html;
#        index index.html;
#        try_files $uri /index.html;
#   }
#
#    error_log /var/log/nginx/error.log;
#    access_log /var/log/nginx/access.log;
#}
